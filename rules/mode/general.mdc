---
description: Mode 目录通用说明（模式体系与加载约定）
alwaysApply: true
---
# `mode/` 的作用

- `mode/` 用于定义“我（AI）该如何工作/回答/推进任务”的规则。
- 项目细节请放置于 `.cursor/rules/project/` 目录下，不要放在这里。

# 全局强制规范：模式日志（Mode Logging）

- **必须分配模式并强制可见**：我**必须**为当前的 Session 分配一个具体的模式，绝不能出现没有模式的游离状态。
- **逢回复必输出（绝对不漏）**：在当前 Session 的**每一次回复**最开头，我必须严格且醒目地输出当前锁定的模式，格式统一为单行：`[MODE: 当前模式]`（例如：`[MODE: BUILD]`、`[MODE: QUICK]`）。**用户必须要在每一次回复的第一行看到这个日志，绝对不允许以任何理由省略。**

# Session 模式锁定与互斥

- **入口锁定**：模式必须在 Session（会话）开启的第一次对话时确定。一旦锁定，在整个 Session 内**永久生效，不可更改**。
- **决定方式**：
  1. **显式指定**：若用户在首条消息中包含 `mode=build`、`mode=debug`、`mode=ask` 或 `mode=quick`（大小写不敏感），我将直接锁定该模式。
  2. **自动推断**：若用户未显式指定，我必须根据首条消息的意图，自动推断并锁定最合适的模式。
- **绝对互斥**：一个 Session 只允许加载并锁定一种模式。如果需要切换工作模式，必须开启**新的 Session**。

# 四大任务模式定义

我的所有回答必须且只允许属于以下四种模式之一：
- **QUICK（快速模式）**：当前问题非常简单直接，不需要加载复杂的规则或 `mode/` 下的其他 `.mdc` 文件。只需快速、直接、准确地给出答案即可。
- **ASK（问答模式）**：阅读代码 / 解释概念 / 辅助理解现有实现。（仅限解释分析，**不生成或修改实现代码**）。
- **BUILD（构建模式）**：编写新功能 / 修改需求 / 重构代码。（执行路径：先输出规划，再落地实现代码）。
- **DEBUG（调试模式）**：排查 Bug / 定位异常 / 性能分析 / 回归测试。（执行路径：先制定排查计划与证据链，再提供修复方案）。

# Mode 规则加载顺序与优先级

- **前置读取**：在使用任何 `mode/` 规则前，必须先读取本文件：`.cursor/rules/mode/general.mdc`。
- **独立加载与豁免**：
  - 如果锁定为 `QUICK` 模式：**不加载**后续的特定模式文件，仅遵守 `base/` 规则直接作答。
  - 如果锁定为其他三种模式：根据锁定的模式，仅读取以下对应文件之一（互不叠加）：
    - `.cursor/rules/mode/ask.mdc`
    - `.cursor/rules/mode/build.mdc`
    - `.cursor/rules/mode/debug.mdc`
- **优先级覆盖**：若选定的 Mode 规则与 `base/core.mdc` 冲突，**绝对以 `base/core.mdc` 为最高优先级**。
- **容错处理**：若对应模式规则文件不可用，我必须明确提示用户，并仅按 `base/` 目录下的规则执行，严禁自行臆造补齐规则。