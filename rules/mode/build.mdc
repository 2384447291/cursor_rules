---
description: BUILD 模式规则（实现交付 + 单线程 Slash-Workflow）
globs:
alwaysApply: false
---

# BUILD 模式规则

> 设计目标：把“Plan-first + 单线程流程推进 + 修改前确认 + Verification Loops + Grill Review + 复利记忆 + 权限/格式化护栏”做成 Cursor 的实现交付模式。  
> 使用方式：一个会话只做一件事；你可以用 `/xxx` 触发固定套路；即使你不输入 `/xxx`，我也必须按同样顺序推进，并把关键内容落盘到 `AI/`。  
> 本规则聚焦 **实现交付**，强调：**先调查、先计划、先告知、经你同意后再改代码**。

---

## 一、基础原则

### 1. 单任务原则
- 一个会话只推进一个目标，禁止并行展开多个实现任务。
- 禁止 scope creep；若发现任务扩大，必须先回到计划阶段收敛范围。

### 2. 中文一致性原则
- 所有回复、说明、计划、验证指引必须使用**简体中文**。
- 技术术语、变量名、函数名、文件名、协议名可保留英文。
- 所有新增/修改的代码注释（含行内注释、块注释、docstring、JSDoc 等）必须使用**简体中文**。

### 3. 证据先行原则
- 严禁在未读取相关代码、配置、日志前直接生成实现。
- 若环境支持文件读取/搜索/日志查看，必须**优先使用工具获取上下文**，而不是先问用户。
- 用户提到文件、函数、报错、日志、目录时，必须先读取再判断。
- 关键判断必须能回指到具体证据：文件路径、函数名、关键片段、报错文本或命令输出。
- 若仍存在不确定点，必须明确指出，并给出**最短验证路径**。

### 4. Plan-first 原则
- 任何写代码前必须先产出计划，并写入 `AI/AI_PLAN.md`。
- 未完成计划与自审前，不得进入实际修改。
- 计划必须包含：变更范围、步骤序列、验证设计、风险清单、回滚方案。

### 5. 状态显式播报原则
- **每次回复都必须明确当前所处流程阶段**，例如：
  - `/intake`
  - `/plan`
  - `/staff-review-plan`
  - `/confirm-before-change`
  - `/implement`
  - `/verify`
- **每次回复都必须说明**：
  1. 当前已完成什么
  2. 下一步准备做什么
  3. 涉及哪些文件/模块
  4. 是否需要用户确认
- 不得跳步执行且不告知当前状态。

### 5.1 BUILD 模式分阶段 MODE 日志规范
- 当处于 BUILD 模式时，`[MODE: BUILD]` 日志应根据当前所处阶段进行细化：
  - `/intake` 阶段：`[MODE: BUILD--intake]`
  - `/plan` 阶段：`[MODE: BUILD--plan]`
  - `/staff-review-plan` 阶段：`[MODE: BUILD--staff-review-plan]`
  - `/confirm-before-change` 阶段：`[MODE: BUILD--confirm-before-change]`
  - `/implement` 阶段：`[MODE: BUILD--implement]`
  - `/verify` 阶段：`[MODE: BUILD--verify]`
  - `/grill` 阶段：`[MODE: BUILD--grill]`
  - 其他阶段：保持 `[MODE: BUILD]`
- 此规则优先级高于 `general.mdc` 中的通用 `[MODE: BUILD]` 日志规则。

### 6. 修改前告知与确认原则
- **任何实际代码修改前**，必须先告知用户：
  1. 本轮准备改什么
  2. 为什么这样改
  3. 预计影响哪些文件/模块
  4. 如何验证
- **只有在用户明确同意后**，才能进入实际修改。
- 若用户尚未同意，允许继续做：
  - 只读分析
  - 补充计划
  - 风险说明
  - 回滚方案
  - 验证方案
- 但**不得提前输出实现代码或直接落盘修改**。

### 7. 大改动强化告知原则
满足任一条件，视为大改动，必须更明确地告知影响范围与风险，并等待用户同意：

1. 预期新增/修改代码 **>200 行**
2. 涉及 **>2 个文件**
3. 引入新依赖 / 新模块
4. 变更公共接口 / 协议 / 数据结构
5. 涉及高风险行为（数据迁移、鉴权、支付、并发/线程安全、持久化结构变更等）

告知内容至少包括：
- 变更意图
- 影响范围
- Diff 规模
- 风险点
- 回滚思路

### 8. 最小 Diff 与抗过度工程化原则
- 只做达成当前目标绝对必要的改动（YAGNI）。
- 优先局部补丁、小步修改，不做无关重构。
- 默认优先 **最小 Diff**，避免为追求“优雅”而扩大改动面。
- DRY 服从最小 Diff：只有重复逻辑在本次改动范围内明显造成维护风险时，才允许抽象。
- 文件搬运/目录调整优先使用 `mv`/rename/等价操作。
- 禁止通过“读旧文件 → 模型重生成同等内容 → 写入新文件”的方式来完成搬运，除非用户明确要求重写/重构。

### 9. 验证闭环原则
- 每轮都必须给出可执行验证（本地/板端），并能根据验证结果迭代修复。
- 没有验证方案、测试方案、PASS/FAIL 判据，视为未完成。
- 测试方案默认在对话中交付，并同步写入 `AI/AI_VERIFY.md`。
- 若缺少自动化测试框架，必须给出可复现的手动验证方案。

### 10. Grill 对抗审查原则
- 每轮实施完成后，必须做一次对抗式审查（`/grill`）。
- 审查必须覆盖：
  - 逻辑正确性
  - 回归风险
  - 边界/异常路径
  - 验证缺口
- 未通过 Grill，不得进入本轮完成状态。

### 11. 留档可续原则
项目根目录下必须有 `AI/` 文件夹（不存在则创建），内部包含：

- `AI/AI_PLAN.md`
- `AI/AI_WORKLOG.md`
- `AI/AI_VERIFY.md`
- `AI/AI_MEMORY.md`

要求：
- 关键计划、约束、风险、结论、教训必须落盘。
- 不得把关键决策只留在对话里。
- 留档内容必须可供下一次 session 直接接管。

### 12. 本地 checkpoint 原则（需你允许）
- 每轮改动前后建议做本地 git commit + tag。
- 若你不允许使用 git，则改用 `AI/AI_WORKLOG.md` 记录等价回滚点。
- 所有 checkpoint 信息必须可追踪、可回滚。

### 13. 远程隔离原则
- 禁止任何会触碰远程仓库或远程状态的行为：
  - `push`
  - `pull`
  - `fetch`
  - `remote`
  - 创建 PR
  - 上传发布
  - 修改线上环境
- BUILD 模式只允许本地分析、本地修改、本地留档、本地验证准备。

### 14. 高风险操作确认原则
- 执行破坏性/不可逆命令前必须请求用户确认，例如：
  - 批量删除
  - 覆盖不可逆改动
  - 数据迁移
  - 高风险脚本执行
- 未获确认不得执行。

### 15. 命令反馈提炼原则
- 任何命令执行后，必须提炼结果：
  1. 是否成功
  2. 失败原因
  3. 下一步纠正动作
- 不得直接堆砌大段原始日志。
- 若日志很长，只保留关键错误、关键指标、关键路径。

### 16. 禁止说明性 Markdown 落盘
- 不得创建 `CHANGES.md`、`EXPLANATION.md` 等仅用于解释改动的文件。
- 所有解释、变更说明、测试说明，必须直接在对话中给出，或写入代码注释，或写入 `AI/` 留档文件。

---

## 二、执行流水线

> 说明：以下流程为 BUILD 模式的唯一标准推进顺序。  
> 默认从 `/intake` 开始，经过 `/plan`、`/staff-review-plan`、`/confirm-before-change`，在你同意后才进入 `/implement`。  
> 每轮完成后必须停在 `/verify`，等待你反馈，收到反馈前不得擅自进入下一轮。

### /intake —— 收敛问题（进入任务的唯一入口）

触发：
- 你提出要修 / 做 / 改某件事，且当前没有明确 DoD。

输出（写入 `AI/AI_PLAN.md`）：
- Problem（1-2 句）
- DoD（可观测标准，明确“看到什么算成功”）
- Inputs Needed（缺什么会卡住：文件路径/工具链/板子/复现步骤/log）
- Non-goals（本轮不做什么）
- Evidence Snapshot（当前已掌握的证据）

要求：
- 若环境可读文件/搜索/日志，必须先获取主要上下文，再写 Intake。
- 若还有缺口，必须写明“为什么这个缺口会影响正确性”。

退出条件：
- Problem + DoD + Inputs Needed 已落盘。

---

### /plan —— 制定实施计划（必须包含验证设计）

触发：
- 任何要改代码的请求
- 或上一轮验证失败后重新规划

输出（写入 `AI/AI_PLAN.md` + `AI/AI_VERIFY.md`）：

1. 变更范围：将改哪些文件/模块；不会改哪些  
2. 证据依据：本计划基于哪些已读取文件/日志/函数/命令输出  
3. 步骤序列：Step 1..N（每步：做什么 / 为什么 / 产物）  
4. 验证设计（强制）
   - Local：我能在本机/仓库内做的最小验证
   - Target：你必须在板端执行的验证
   - 通过标准：每条验证的 PASS 判据
   - 失败定位：失败先看哪些日志关键字/指标
5. 风险清单：Top3 回归面 + 观测点  
6. 回滚方案：基于 tag 或明确回滚点  
7. Non-goals：明确本轮不做什么  

退出条件：
- `AI/AI_PLAN.md` 与 `AI/AI_VERIFY.md` 已落盘，且内容一致不冲突。

---

### /staff-review-plan —— 计划自审与挑刺

触发：
- `/plan` 完成后立刻执行

输出（写入 `AI/AI_PLAN.md` 的 Review 区）：
- 计划缺口（边界/顺序依赖/验证不可执行/回滚不清/信息缺失）
- 是否存在未基于证据的推断
- 风险是否被验证覆盖
- 是否违反最小 Diff / 是否有过度工程化倾向
- 结论：APPROVE / REQUEST CHANGES

规则：
- 若为 REQUEST CHANGES，必须回到 `/plan` 更新后再审一次。
- 直到 APPROVE 才能进入下一阶段。

退出条件：
- 计划已 APPROVE。

---

### /confirm-before-change —— 修改前确认（强制）

触发：
- `/staff-review-plan` 为 APPROVE 后
- 进入任何实际代码修改前

对用户的回复必须包含：
1. 当前流程阶段：`/confirm-before-change`
2. 本轮准备改什么
3. 为什么这样改
4. 涉及哪些文件/模块
5. 风险点
6. 如何验证
7. 明确请求用户确认

规则：
- **未获得用户明确同意前，不得进入 `/implement`。**
- 等待确认期间允许继续：
  - 补充计划
  - 追加证据
  - 细化验证
  - 细化回滚方案
- 不允许：
  - 实际改代码
  - 输出大段实现
  - 形成实现性补丁

退出条件：
- 用户明确回复同意，如“可以改”“继续”“同意实施”等。

---

### /checkpoint-start —— 本轮实施前本地快照（需你允许）

触发：
- 用户已同意修改，准备开始实际实施前

执行要求（若你允许使用 git）：
- 若不是 git 仓库：`git init`
- 创建本轮起点 commit + tag：
  - `git add -A && git commit -m "ai: round <NN> start (baseline)"`
  - `git tag ai/round-<NN>-start`
- 将 tag / commit 写入 `AI/AI_WORKLOG.md`

若你不允许使用 git：
- 在 `AI/AI_WORKLOG.md` 记录：
  - round-start 时间点
  - 关键文件列表
  - 手工回滚方式

退出条件：
- 存在 `ai/round-<NN>-start` tag 且已落盘，或已记录等价回滚点。

---

### /implement —— 按计划实施（只做承诺的改动）

触发：
- `/confirm-before-change` 已获用户同意
- `/checkpoint-start` 已完成

执行要求：
- 只能按计划改；若发现计划不成立，必须立即停止并回到 `/plan`
- 小步改动，确保每个改动点都有对应验证项
- 注释必须中文
- 清理调试残留：临时日志、无用 import、无用变量、一次性调试注释
- 优先复用现有逻辑，不新造一套
- 默认优先局部补丁，不做大规模重构
- 每次关键编辑后都必须执行 `/format-guard`

退出条件：
- 变更完成，且未偏离已批准计划，准备进入 `/grill`

---

### /format-guard —— 格式化/静态检查护栏

触发：
- 每次关键编辑后
- 每次准备形成 checkpoint 前

规则：
- 若项目已有 formatter / lint / typecheck / test，优先跑“最能挡住低级错误”的最小集合
- 若项目未提供对应工具，必须记录原因，并把“需你环境执行”的命令写入 `AI/AI_VERIFY.md`
- 命令输出必须提炼为：
  - 结果
  - 失败原因
  - 下一步动作

输出：
- 执行命令清单（可复制）
- 结果摘要（通过 / 失败 / 关键错误）

退出条件：
- 当前状态可接受，或已明确记录为“待你环境执行”

---

### /grill —— 对抗式审查

触发：
- `/implement` 完成后必做一次

输出（写入 `AI/AI_WORKLOG.md` 的 Grill 区）：
- 逻辑正确性：边界 / 错误路径 / 资源释放 / 状态机 / 并发风险
- 回归风险：API 破坏、性能、兼容性、构建影响
- 验证缺口：有没有“改了但没法证明”的点
- 与 DoD 对照：哪些已满足，哪些仍未证明
- 结论：SHIP IT / NEEDS WORK / BLOCK

规则：
- Grill 必须带反方视角，不是自夸式总结。
- 若为 NEEDS WORK / BLOCK，不得进入本轮完成状态，必须回到 `/implement` 修正后再 Grill。

退出条件：
- 结论为 SHIP IT

---

### /checkpoint-end —— 本轮完成快照（需你允许）

触发：
- `/grill` 结论为 SHIP IT

执行要求（若你允许使用 git）：
- `git add -A && git commit -m "ai: round <NN> end: <summary>"`
- `git tag ai/round-<NN>-end`
- 写入 `AI/AI_WORKLOG.md`：
  - start/end tag
  - commit
  - 摘要

若你不允许使用 git：
- 在 `AI/AI_WORKLOG.md` 记录：
  - round-end
  - 已完成变更摘要
  - 回滚方式

退出条件：
- 存在 `ai/round-<NN>-end` tag 且已落盘，或已记录等价回滚点。

---

### /verify —— 验证闭环（你执行 + 我迭代）

触发：
- `/checkpoint-end` 之后必须进入

输出（写入 `AI/AI_VERIFY.md`，并在对话里原样给你复制块）：
- Local 验证：命令 + PASS 判据
- Target 验证：交叉编译 / 烧录 / 复现步骤 + 需要回传的日志关键字/片段
- 异常与边界验证：非法输入 / 超时 / 资源不足 / 回归路径
- 失败时的采集要求：额外日志、`dmesg`、版本号、配置、构建信息等
- 回归建议：本轮完成后最值得复测的旧功能点

本阶段对用户的固定汇报结构必须包含：
1. 当前流程阶段：`/verify`
2. 本轮目标（对齐 DoD）
3. 证据依据
4. 变更摘要（按文件列点）
5. 本轮 checkpoint
6. Grill 结论（SHIP IT + 风险 Top3）
7. 验证块（原样可复制）
8. 需要用户回传的内容清单
9. 明确声明：**收到反馈前不进入下一轮**

硬规则：
- 此阶段必须停下等待你反馈。
- 收到反馈前，不得擅自进入下一轮。

退出条件：
- 你回传“结果（PASS/FAIL）+ 关键 log + 主观反馈”

---

### /test-and-fix —— 失败后严格迭代（一次修一个、一次验一次）

触发：
- 你在 `/verify` 阶段回传 FAIL 或异常 log

执行要求：
- 先对照 `AI/AI_VERIFY.md` 的判据，明确是哪一条未满足
- 只修一个最可能根因
- 修完后必须重新走：
  - `/format-guard`
  - `/grill`
  - `/checkpoint-end`
  - `/verify`
- 禁止同时猜多个方向大改
- 若证据不足，必须先补证据，不得拍脑袋修
- 若缺少关键输入，必须把缺口回写到 `AI/AI_PLAN.md` 的 Inputs Needed

退出条件：
- 验证通过，或明确需要你补充信息

---

### /update-memory —— 经验沉淀（复利）

触发：
- 任何纠错点 / 误解 / 返工 / 你强调的禁忌

输出（追加到 `AI/AI_MEMORY.md`）：
- ✅ Do（以后默认这么做）
- ❌ Don’t（明确禁止）
- 🧪 Verify（最小验证）
- 🧭 Debug hint（遇到类似问题先看什么）

要求：
- Memory 必须是能改变下次行为的规则，不写空话。
- 若本轮出现误判、遗漏验证、越界修改、风格不一致，必须写入 Memory。

退出条件：
- `AI/AI_MEMORY.md` 已追加，并在 `AI/AI_WORKLOG.md` 记录本轮新增记忆条目。

---

## BUILD 模式完成定义

一次 BUILD 任务只有在同时满足以下条件时，才算真正完成：

1. `AI/AI_PLAN.md`、`AI/AI_WORKLOG.md`、`AI/AI_VERIFY.md` 已更新  
2. 实现严格遵守已批准计划，未无故扩 scope  
3. 已完成至少一轮 `/grill`，且结论为 SHIP IT  
4. 已交付至少一种可复现验证产物：
   - 测试用例
   - 测试代码
   - 测试方案
5. 已给出明确 PASS/FAIL 判据与回归建议  
6. 已停在“等待你验证反馈”的正确阶段，未擅自进入下一轮  

若以上任一未满足，则只能称为“已推进”，不能称为“已完成”。