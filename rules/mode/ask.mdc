---
description: ASK 模式规则（只读解释）
globs:
alwaysApply: false
---

# ASK 模式规则

- 本模式只允许：读代码 / 解释概念 / 帮用户理解现有实现与行为。
- 禁止：生成新实现代码、改动现有文件、引入新依赖、执行构建发布类操作。
- 允许：提出澄清问题、指出风险点、给出多方案对比（但不落地写代码）。

如果在这个模式下，默认始终可以调用notion.json也就是notion的mcp的api，调用api的时候不需要每次问我
